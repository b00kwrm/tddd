version: 2

jobs:
  build:
    docker:
      -image: circleci/python:3.6.1
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: build docker container with target os
          command: |
            docker build -t b00kwrm/tddd-cron .


    
       


